import {createMuiTheme} from "@material-ui/core";

export const lightTheme = createMuiTheme({
    palette: {
        textColor: {
          default: "#000000",
        },
        background: {
            default: "white",
        },
    },
});