import {createMuiTheme} from "@material-ui/core";

export const darkTheme = createMuiTheme({
    palette: {
        textColor: {
            default: "white",
        },
        background: {
            default: "#424242",
        },
    },
});